App:
1.Create a PWA app for coloring activity
2.Allow user to choose between the coloring pdf already uploaded or upload a new pdf.
3.Also Allow user to upload an image and create an outline from it.
Outline feature:
1.create a outline from the image uploaded by the user
2.make sure the outline is smooth and proper
3.make sure the outline is proper so that kids can do a coloring activity
4.make sure the outlines are closed properly for coloring.
5.allow user to choose different colors.
6.simulate sound when coloring the outline.

Feature 1.0 
1. Dynamically change the size to 950 or above available size only when in mobile view.
2. Dynamically hide the header bar, show a toggle icon on the top to show/hide the header bar, so that the editor space uses entire screen height and width.
3. Show a toggle button on the right side for the toolbar, so that the toolbar can be hidden and shown in full screen mode.
